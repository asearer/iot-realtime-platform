syntax = "proto3";

package telemetry;

option go_package = "go-processor/internal/proto";

message Telemetry {
    string device_id = 1;
    int64 ts = 2; // epoch ms
    map<string, double> metrics = 3;
    bytes raw = 4;
}
